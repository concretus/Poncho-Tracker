general:
  build_dir: app
  artifacts:
    - ./app/server/covarge

machine:
  node:
    version: 6.2.0

dependencies:
  override:
    - npm install --dev

notify:
  webhooks:
    - url: https://coveralls.io/webhook
