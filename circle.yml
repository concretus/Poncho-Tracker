general:
  build_dir: app
  artifacts:
    - ./app/server/covarge

machine:
  node:
    version: 6.2.0

dependencies:
  override:
    - npm install --dev

database:
  pre:
    - psql
    - CREATE DATABASE poncho_db_test;
    - CREATE TABLE users;
    - \q

test:
  post:
    - bash <(curl -s https://codecov.io/bash)